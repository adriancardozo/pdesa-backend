import { UserRequest } from 'src/auth/type/user-request.type';

export const request = { user: { id: '1' } } as UserRequest;

export const q = 'product query';

export const productParam = { ml_id: 'idMl' };
