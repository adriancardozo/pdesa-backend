import { UserRequest } from 'src/auth/type/user-request.type';

export const purchaseParam = { idMl: 'ML123456' };

export const dto = { amount: 1 };

export const request = { user: { id: 'user1' } } as UserRequest;
